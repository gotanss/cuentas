spring:
  application:
    name: Cuentas
  datasource:
    url: jdbc:postgresql://localhost:5332/cuentadb
    password: password
    username: amigoscode
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
server:
  port: 8080